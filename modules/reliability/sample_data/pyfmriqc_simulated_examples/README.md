# Simulated pyfMRIqc example outputs

These six PNGs are `pyfMRIqc`'s own generated output, run locally on `pyfMRIqc`'s own bundled *simulated* example files (motion artifact, local signal loss, global intensity loss). They are bundled here because, unlike the real fMRI Open QC Project subject data this journey also reads (never bundled, read from a local path you provide), these come from synthetic, purpose-built demonstration signals that ship with `pyfMRIqc` itself for exactly this reason.

No real research participant data appears in these images.

## Provenance

Generated by running `pyfMRIqc.py` (unmodified) on its own `examples/` files:

- `pyfMRIqc_example_motion.nii.gz` + `.par` -> `motion_plots.png`, `motion_mean.png`
- `pyfMRIqc_example_local_signal_loss.nii.gz` (shipped as a split `.001`/`.002` archive, concatenated before running) -> `local_signal_loss_plots.png`, `local_signal_loss_mean.png`
- `pyfMRIqc_example_volume_intensity_loss.nii.gz` -> `global_intensity_loss_plots.png`, `global_intensity_loss_mean.png`

## Attribution

`pyfMRIqc` is developed by Michael Lindner and Brendan Williams, University of Reading, School of Psychology and Clinical Language Sciences, Centre for Integrative Neuroscience and Neurodynamics. Source: https://github.com/DrMichaelLindner/pyfMRIqc (GPLv3). `pyfMRIqc`'s own code is not vendored into this repository; only its output images on its own example inputs are bundled here.

Citation: Williams, B., & Lindner, M. (2020). pyfMRIqc: A Software Package for Raw fMRI Data Quality Assurance. *Journal of Open Research Software*, 8(1), 23. https://doi.org/10.5334/jors.280
